{
  "name": "ubuntu-website",
  "version": "0.0.1",
  "description": "Django website for ubuntu.com",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass --include-path node_modules --output static/css static/css",
    "watch": "node-sass --include-path node_modules --watch --source-map true --recursive static/css/styles.scss static/css/styles.css"
  },
  "keywords": [
    "website",
    "ubuntu"
  ],
  "author": "Canonical webteam",
  "license": "LGPL v3",
  "dependencies": {
    "node-sass": "^4.5.0",
    "ubuntu-vanilla-theme": "0.0.35"
  }
}
